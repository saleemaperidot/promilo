import 'package:flutter/material.dart';

class PromiloHome extends StatelessWidget {
  const PromiloHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Container(
        child: Text("Home"),
      ),
    ));
  }
}
